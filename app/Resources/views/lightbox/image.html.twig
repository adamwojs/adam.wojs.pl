{% set image_field_identifier = ez_first_filled_image_field_identifier(content) %}

{% if image_field_identifier is not null %}
    {{ ez_render_field(content, image_field_identifier,{
        parameters: {
            original_alias: 'original',
            thumbnail_alias: 'lightbox_thumbnail'
        },
        template: ':fields:lightbox.html.twig'
    }) }}
{% endif %}
